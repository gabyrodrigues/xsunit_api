#This controller was created to implement solutions for v1 only, this is a way to avoid possible conflicts when implementing 
#other versions, so all the other controllers created for v1 are inheriting for this controller rather than application_controller
class Api::V1::ApiController < ApplicationController
	# Métodos globais

end